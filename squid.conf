# --- Port definitions ---
acl SSL_ports port 443
acl Safe_ports port 80
acl Safe_ports port 443
acl Safe_ports port 8080
acl CONNECT method CONNECT

# # --- Domain allowlist ---
# # LLM providers
# acl allowed_domains dstdomain .openai.com
# acl allowed_domains dstdomain .anthropic.com
# acl allowed_domains dstdomain .googleapis.com
# acl allowed_domains dstdomain .mistral.ai
# acl allowed_domains dstdomain .blob.core.windows.net

# # Observability / data sources
# acl allowed_domains dstdomain .datadoghq.com
# acl allowed_domains dstdomain .datadoghq.eu
# acl allowed_domains dstdomain .newrelic.com
# acl allowed_domains dstdomain .elastic.co
# acl allowed_domains dstdomain .pagerduty.com
# acl allowed_domains dstdomain .grafana.com
# acl allowed_domains dstdomain .grafana.net
# acl allowed_domains dstdomain .observeinc.com
# acl allowed_domains dstdomain .openobserve.ai
# acl allowed_domains dstdomain .sentry.io

# # Integrations
# acl allowed_domains dstdomain .slack.com
# acl allowed_domains dstdomain .github.com
# acl allowed_domains dstdomain .githubusercontent.com
# acl allowed_domains dstdomain .atlassian.net
# acl allowed_domains dstdomain .atlassian.com




# --- Reverse proxy settings ---
http_port 8080 accel defaultsite=web vhost
cache_peer web parent 8000 0 no-query originserver login=PASS name=webapp
request_header_access Authorization allow all
acl reverse_traffic localport 8080
cache_peer_access webapp allow reverse_traffic
never_direct allow reverse_traffic

# --- Access rules ---
http_access deny !Safe_ports
http_access deny CONNECT !SSL_ports
# http_access allow CONNECT allowed_domains
# http_access allow allowed_domains
http_access allow reverse_traffic
http_access allow all
# http_access deny all

# --- Forward proxy listener ---
http_port 3128

# --- Logging (to stdout for docker logs) ---
access_log stdio:/dev/stdout
cache_log stdio:/dev/stderr